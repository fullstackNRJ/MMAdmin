import React from 'react';
import {Button} from 'react-native';

const MMPrimaryButton = ({title, onPress}) => {
  return <Button title={title} onPress={onPress} />;
};

export default MMPrimaryButton;
